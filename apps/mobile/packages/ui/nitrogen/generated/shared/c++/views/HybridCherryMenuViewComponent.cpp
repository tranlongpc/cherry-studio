///
/// HybridCherryMenuViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridCherryMenuViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::cherrystudio::ui::views {

  extern const char HybridCherryMenuViewComponentName[] = "CherryMenuView";

  HybridCherryMenuViewProps::HybridCherryMenuViewProps(const react::PropsParserContext& context,
                                                       const HybridCherryMenuViewProps& sourceProps,
                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    items([&]() -> CachedProp<std::vector<NativeMenuItem>> {
      try {
        const react::RawValue* rawValue = rawProps.at("items", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.items;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<NativeMenuItem>>::fromRawValue(*runtime, value, sourceProps.items);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryMenuView.items: ") + exc.what());
      }
    }()),
    onAction([&]() -> CachedProp<std::function<void(const std::string& /* id */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onAction", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onAction;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const std::string& /* id */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onAction);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryMenuView.onAction: ") + exc.what());
      }
    }()),
    trigger([&]() -> CachedProp<NativeMenuTrigger> {
      try {
        const react::RawValue* rawValue = rawProps.at("trigger", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.trigger;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<NativeMenuTrigger>::fromRawValue(*runtime, value, sourceProps.trigger);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryMenuView.trigger: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridCherryMenuViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridCherryMenuViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("CherryMenuView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridCherryMenuViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("items"): return true;
      case hashString("onAction"): return true;
      case hashString("trigger"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridCherryMenuViewComponentDescriptor::HybridCherryMenuViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridCherryMenuViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                          const std::shared_ptr<const react::Props>& props,
                                                                                          react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridCherryMenuViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridCherryMenuViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridCherryMenuViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridCherryMenuViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridCherryMenuViewProps>& props = std::const_pointer_cast<HybridCherryMenuViewProps>(constProps);
    HybridCherryMenuViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::cherrystudio::ui::views
